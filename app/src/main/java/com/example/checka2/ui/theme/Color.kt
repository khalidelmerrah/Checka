package com.example.checka2.ui.theme

import androidx.compose.ui.graphics.Color

val WarmCream = Color(0xFFFAF7F2) // Background
val OffWhite = Color(0xFFF4F1EA) // This color was not explicitly changed or removed in the instruction, so it remains.
val WarmGray = Color(0xFFE8E3DC)   // Board
val WarmGrayLight = Color(0xFFF4F1ED) // Cell

val Terracotta = Color(0xFFE07856) // P1
val Sage = Color(0xFF7B9E89)       // P2

val WarmCharcoal = Color(0xFF292524) // Keeping alias for existing usages or replacing
val LighterCharcoal = Color(0xFF3E3B36) // This color was not explicitly changed or removed in the instruction, so it remains.
val Stone800 = Color(0xFF292524)   // Buttons/Text
val TextDark = Color(0xFF201D1A) // This color was not explicitly changed or removed in the instruction, so it remains.
val TextLight = Color(0xFFEDEAE6) // This color was not explicitly changed or removed in the instruction, so it remains.

// Functional colors
val ValidMove = Color(0x3381B29A)
val InvalidOp = Color(0xFFE76F51)
